import type { Channel } from "../interfaces/Channel";

/**
 * This represents an up to date list of all supported {@link Channel} 
 * namespace's.
 */
export enum Namespace {
    Process,
    Project,
    Settings,
    Component,
}